import {useState} from 'react'

function today() {
  // active reminders (stored in an array)
  const [task, setTask] = useState([]);
  
  // Create new reminder
  const [newTask, setNewTask] = useState('');

  return (
    <>
      
    </>
  )
}

export default today